server:
  port: 8390
#  servlet:
#    session:
#      timeout: 1m
#  reactive:
#    session:
#      timeout: 1m


spring:
  profiles:
    active: dev # 디폴트 환경

  # ===============================
  # = Thymeleaf configurations
  # ===============================
  thymeleaf:
    mode: HTML
    cache: false
    encoding: UTF-8
    prefix: classpath:/templates/

  web:
    resources:
      static-locations: classpath:/static/
      cache:
        cache-control:
          max-age: 0


